# Infrastructure layer

Adapters for external systems: SQLite, files, TTS, SMS, Android Keystore, Capacitor plugins, and future network providers.

Infrastructure may depend on domain or application contracts, but it must not import presentation pages or components.
