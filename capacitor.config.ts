import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.treecommunicator.app',
  appName: 'Tree Communicator',
  webDir: 'www'
};

export default config;
